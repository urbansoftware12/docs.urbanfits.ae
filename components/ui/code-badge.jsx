import { incons } from "@/app/layout";
export default function CodeBadage({ children, color = "text-orange-500" }) { return <code className={incons.className + ` self-start ${color} px-1.5 py-0.5 bg-gray-200 dark:bg-white/20 rounded-md leading-tight`}>{children}</code> }